{
  "name": "ts-mergesort-tutorial",
  "version": "0.0.0",
  "description": "ts-mergesort-tutorial",
  "main": "app.js",
  "author": {
    "name": ""
  },
  "scripts": {
    "start": "node build/app",
    "build": "tsc --build",
    "buildrun": "npm run build && npm run start",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@types/node": "^17.0.8",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.4"
  }
}
